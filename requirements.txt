# Phase A (download + explore TOXRIC, clone KPGT, inspect schema)
requests>=2.31
pandas>=2.0
python-dotenv>=1.0
tqdm>=4.65
jupyter>=1.0
ipykernel>=6.25

# Phase B (training)
rdkit>=2023.9
torch==2.3.0
# DGL 2.2.1 graphbolt tops out at PyTorch 2.3.0; torchdata must stay at 0.7.x for datapipes support.
# Install DGL from its own wheel server: pip install dgl==2.2.1 -f https://data.dgl.ai/wheels/repo.html
dgl==2.2.1
torchdata==0.7.1
dgllife>=0.3.2
scikit-learn>=1.3
numpy>=1.24
scipy>=1.10

# Phase C (LLM explainer)
anthropic>=0.30
azure-identity>=1.15

# Phase F (safer alternatives finder + Streamlit UI)
matplotlib>=3.7
seaborn>=0.13
streamlit>=1.35

# Phase D (Azure ML)
# azure-ai-ml>=1.12
