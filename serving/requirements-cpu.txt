# CPU-only inference deps for the self-hosted VM (Standard_B2s).
# Used by serving/setup_vm.sh. Much smaller than azure/env.yml (no CUDA).

# --- ML stack (CPU wheels — no CUDA) ---
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.2

--find-links https://data.dgl.ai/wheels/repo.html
dgl==1.1.3

dgllife>=0.3.2

# --- Chemistry ---
rdkit>=2023.9.5

# --- KPGT internals expect this ---
transformers>=4.30.0

# --- Data / scientific ---
pandas>=2.0
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3

# --- Web service ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5

# --- Explainer + auth ---
anthropic>=0.30
azure-identity>=1.15

# --- Misc ---
python-dotenv>=1.0
requests>=2.31
tqdm>=4.65
